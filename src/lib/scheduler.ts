import type { Goal, Milestone, LifeBlock, ScheduledBlock } from '../types'

export function generateWeekSchedule(
  _weekOf: string,
  _goals: Goal[],
  _milestones: Milestone[],
  _lifeBlocks: LifeBlock[]
): ScheduledBlock[] {
  return []
}
